import { PluralRuDirective } from './plural-ru.directive';

describe('PluralRuDirective', () => {
  it('should create an instance', () => {
    const directive = new PluralRuDirective();
    expect(directive).toBeTruthy();
  });
});
